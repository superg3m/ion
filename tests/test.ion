var x: int = 8;

fn do_something(a: int, b: []string) -> void {
    println(a);
}

fn main() -> void {
    var y := 3 + 4.3;
    println(y);

    // do_something(1000);

    println(x);
    // println(x + 4);
}