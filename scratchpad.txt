- NOTES -
    1. Make sure Vector has a macro 
    Vector(int) data; instad of int* data;

-- Jovanni's Vision -- 
// Basic Program (subject to change!)
enum TypeID {
    INVALID = -1;

    INTEGER = 5;
    FLOAT;      // 6
}

type_group TypeIDMask = u32|f32

// Maybe it shoulnd't be a runtime type check maybe it should be like a codegen thing
// @switch($T)
// @ is like a codegen symbol
func get_type_value(value: $T/TypeIDMask) -> TypeID, $T {
    switch ($T) {
        case == u32 {
            return INTEGER, value;
        } break;

        case == f32 {
            return FLOAT, value;
        }

        /* NOT ALLOWED BECAUSE OF TypeIDMask
        case == string {
            return FLOAT, value;
        }
        */
    }

    return INVALID, value;
}


func main() -> s32 {
    var typeid, _ := get_type_value(5);
    var typeid2, value := get_type_value(6.5);


    return 0;
}
----------------------